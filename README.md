# How to test your MCP


# Prerequesits
Install the mcp inspector package globally
Install nodemon
Check your package.json has a "dev" script
`"dev": "nodemon --watch src --exec tsx src/index.ts"`
This allows you to run `npm run dev`.


2. Open the command line and run this (in powershell)
`mcp-inspector npm run dev`